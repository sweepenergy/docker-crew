<%- include("partials/header"); %>
    
<div class="row mb-4">
  <h2>Homepage</h2>
  
</div>
    
<div class="d-flex justify-content-between">

  <div class="row mb-4 mt-4">
        
        <a href="/addStream" class="btn btn-sm btn-outline-secondary" id="CreateNewStreamBtn" >Create New Stream +</a>
  </div>

  <div class="row mb-4 mt-4">
          
          <a href="/login" class="btn btn-sm btn-outline-secondary" id="ExitUIBtn" >Exit UI</a>
  </div>

</div>

<div class="dropdown">
        <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false">
          Streams
        </button>

        
        <ul class="dropdown-menu" aria-labelledby="dropdownMenuButton1">
          
          <% if (streams.length > 0){ %>
            <% streams.forEach(directory => { %>

          <li><a class="dropdown-item" href="#"></a><%= streams.name %></a></li>
          
            <% }) %>
            <%}else{ %>
          <p>There was an error displaying streams...</p>
          <% } %>
        </ul>

        

</div>
    

<div class="table-responsive">
  <table class="table table-striped table-sm table-bordered">
    <thead>
      <tr>
        <th>Name</th>
        <th>Description</th>
        <th>Units</th>
        <th>Type</th>
        <th>Last Updated</th>
        <th>Delete</th>
      </tr>
    </thead>
    <tbody>


        <% if (streams.length > 0){ %>
          <% streams.forEach(directory => { %>

          

        <!-- testing with GET -->
        <form action="/streamData" method="GET">
          <tr>
            <td> <a href="/dbHome/<%= directory._id %>"> <%= streams.name %> </a></td>
            
            
            <td>test</td>
            
            <td>random</td>
            <td>data</td>
            <td>placeholder</td>
            <td>text</td>
            <td>placeholder</td>
            <td>
                    <div class="btn-group" role="group" aria-label="Basic checkbox toggle button group">
                    <input type="checkbox" class="btn-check" id="btncheck1" autocomplete="off">
                    
            </td>
          </tr>
        </form>
        
        <% }) %>
        <%}else{ %>
        <p>There was an error displaying streams...</p>
        <% } %>
    

    </tbody>
  </table>
</div>

<div class="d-flex justify-content-between">

  <div class="row mb-4 mt-4">
        
        <a href="/continue" class="btn btn-sm btn-outline-primary" id="ContinueBtn" >Continue</a>
  </div>

  <div class="row mb-4 mt-4">
          
          <a href="/deleteParameter" class="btn btn-sm btn-outline-primary" id="deleteParameterBtn" >Delete</a>
  </div>

</div>


</main>
</div>
</div>



<%- include("partials/footer"); %>